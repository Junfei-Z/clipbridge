{
  "name": "clipbridge",
  "version": "0.5.0",
  "private": true,
  "description": "A lightweight, local-first text and file bridge with Windows or Mac relay nodes.",
  "type": "module",
  "scripts": {
    "start": "node src/server.mjs",
    "test": "node --test",
    "build:mac": "bash macos/build-app.sh"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "dependencies": {
    "qrcode-generator": "^2.0.4"
  }
}
